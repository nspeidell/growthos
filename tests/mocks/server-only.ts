// Stub for 'server-only' package — used in vitest environment only.
// In production, this package prevents server modules from being imported in client bundles.
// In tests, we just need a no-op so imports don't throw.
export {};
